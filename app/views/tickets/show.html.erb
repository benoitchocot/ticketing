<h1><%= @ticket.title %></h1>
<p><%= @ticket.description.body.to_s.html_safe %></p>
<p>Priorité: <%= @ticket.priority.title %></p>
<p>Statut: <%= @ticket.status.title %></p>
<p><strong>Date de fin :</strong> <%= @ticket.end_date.present? ? l(@ticket.end_date, format: :long) : "Non définie" %></p>


<% if current_user.admin? %>
<!-- Affichage des utilisateurs associés -->
<p>Utilisateurs:
  <% @ticket.users.each do |user| %>
    <%= user.email %>
    <% # Si tu veux afficher plus d'infos comme le nom ou autre, tu peux l'ajouter ici %>
  <% end %>
</p>
<% end %>

<%= link_to "Modifier", edit_ticket_path(@ticket), class: "btn btn-warning" %>
<%= link_to "Retour", tickets_path, class: "btn btn-secondary" %>
